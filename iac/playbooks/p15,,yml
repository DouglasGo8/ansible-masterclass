---
- name: Install Apache2 if RAM > 2GB
  hosts: dev
  become: true

  tasks:
    - name: Installing Apache2
      ansible.builtin.apt:
        name: apache2
        state: present
        update_cache: yes
      when: ansible_memtotal_mb > 2048 # ansible_distribution == "Ubuntu"
